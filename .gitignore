# bank credentials & fetched data — NEVER commit these
feeder/config.json
feeder/state.json
feeder/out/

# personal builds (a Lithifyte file with YOUR transactions embedded)
*personal*.html
*my-*.html

# worker secrets for `wrangler dev` — the OpenRouter key lives here locally
.dev.vars
.dev.vars.*
**/.dev.vars

# local scratch
node_modules/
.DS_Store

# wrangler local cache (account metadata)
.wrangler/

# hourly validate reports (local only)
docs/hourly-runs/
